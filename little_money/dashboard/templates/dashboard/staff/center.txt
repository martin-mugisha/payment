{% load static %}
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Staff Dashboard - {{ page_title }}</title>
    <link rel="stylesheet" href="{% static 'dashboard/css/staff.css' %}" />
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script src="{% static 'dashboard/js/staff.js' %}" defer></script>
  </head>
  <body class="light-mode">
    <div class="sidebar">
      <h2>Staff</h2>
      <nav>
        <a href="{% url 'staff_dashboard' %}"
          ><i class="fas fa-receipt"></i> My Transactions</a
        >
        <a href="{% url 'merchant_list' %}"
          ><i class="fas fa-users"></i> Merchants</a
        >
        <a href="{% url 'payout_management' %}"
          ><i class="fas fa-money-check-alt"></i> Payout Management</a
        >
        <a href="{% url 'dispute_management' %}"
          ><i class="fas fa-exclamation-triangle"></i> Disputes & Chargebacks</a
        >
        <a href="{% url 'refund_processing' %}"
          ><i class="fas fa-undo-alt"></i> Refunds</a
        >
        <a href="{% url 'earnings' %}"
          ><i class="fas fa-receipt"></i> Earnings</a
        >
        <a href="{% url 'failed_transactions' %}"
          ><i class="fas fa-times-circle"></i> Failed Transactions</a
        >
        <a href="{% url 'support_tickets' %}"
          ><i class="fas fa-ticket-alt"></i> Support Tickets</a
        >
        <a href="{% url 'suspicious_activity' %}"
          ><i class="fas fa-shield-alt"></i> Suspicious Alerts</a
        >
        <a href="{% url 'audit_logs' %}"
          ><i class="fas fa-clipboard-list"></i> Audit Log</a
        >
        <a href="{% url 'logout' %}"
          ><i class="fas fa-sign-out-alt"></i> Logout</a
        >
      </nav>
    </div>

    <div class="main-content">
      <header>
        <button id="hamburger-btn" aria-label="Toggle navigation">
          &#9776;
        </button>
        <div class="header-right">
          <div class="toggle-theme">
            <label for="theme-toggle">🌙 Dark Mode</label>
            <input type="checkbox" id="theme-toggle" />
          </div>
          <div class="avatar-dropdown">
            <img
              src="{% static 'dashboard/img/avatar.png' %}"
              alt="User Avatar"
            />
            <div class="dropdown-menu">
              <a href="{% url 'profile' %}">Profile</a>
              <a href="{% url 'logout' %}">Logout</a>
            </div>
          </div>
        </div>
        <h1>Staff Dashboard - {% block title %}{% endblock %}</h1>
      </header>

      <main>{% block content %}{% endblock %}</main>
    </div>
  </body>
</html>

<!-- prettier-ignore -->
{% extends "dashboard/staff/base_staff.html" %}
{% block title %}Audit Log{% endblock %}

{% block content %}
<h2>Audit Log</h2>

<table>
  <thead>
    <tr>
      <th>Staff User</th>
      <th>Action</th>
      <th>Target</th>
      <th>Timestamp</th>
    </tr>
  </thead>
  <tbody>
    {% for log in audit_logs %}
    <tr>
      <td>{{ log.staff_user.username }}</td>
      <td>{{ log.action }}</td>
      <td>{{ log.target }}</td>
      <td>{{ log.timestamp }}</td>
    </tr>
    {% endfor %}
  </tbody>
</table>
{% endblock %}

<!-- prettier-ignore -->
{% extends "dashboard/staff/base_staff.html" %} {% block title %}Disputes &
Chargebacks{% endblock %} {% block content %}
<h2>Disputes & Chargebacks</h2>

<table>
  <thead>
    <tr>
      <th>Transaction ID</th>
      <th>Merchant</th>
      <th>Reason</th>
      <th>Status</th>
      <th>Date</th>
    </tr>
  </thead>
  <tbody>
    {% for dispute in disputes %}
    <tr>
      <td>{{ dispute.transaction_id }}</td>
      <td>{{ dispute.merchant.name }}</td>
      <td>{{ dispute.reason }}</td>
      <td>{{ dispute.status }}</td>
      <td>{{ dispute.date }}</td>
    </tr>
    {% endfor %}
  </tbody>
</table>
{% endblock %}

{% extends "dashboard/staff/base_staff.html" %} {% block title %} Earnings
Overview {% endblock %} {% block content %}
<h2>Earnings Overview</h2>

<!-- Earnings Summary Table -->
<table>
  <thead>
    <tr>
      <th>Period</th>
      <th>Earnings</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Daily</td>
      <td>
        <ul>
          {% for entry in daily_earnings %}
          <li>{{ entry.day|date:"Y-m-d" }}: {{ entry.total_earnings }}</li>
          {% empty %}
          <li>No data</li>
          {% endfor %}
        </ul>
      </td>
    </tr>
    <tr>
      <td>Weekly</td>
      <td>
        <ul>
          {% for entry in weekly_earnings %}
          <li>{{ entry.week|date:"Y-m-d" }}: {{ entry.total_earnings }}</li>
          {% empty %}
          <li>No data</li>
          {% endfor %}
        </ul>
      </td>
    </tr>
    <tr>
      <td>Monthly</td>
      <td>
        <ul>
          {% for entry in monthly_earnings %}
          <li>{{ entry.month|date:"Y-m" }}: {{ entry.total_earnings }}</li>
          {% empty %}
          <li>No data</li>
          {% endfor %}
        </ul>
      </td>
    </tr>
    <tr>
      <td><strong>Total</strong></td>
      <td><strong>{{ total_staff_earnings }}</strong></td>
    </tr>
  </tbody>
</table>

<!-- Withdrawal History Table -->
<h3>Withdrawal History</h3>
<table>
  <thead>
    <tr>
      <th>Amount</th>
      <th>Requested On</th>
      <th>Status</th>
    </tr>
  </thead>
  <tbody>
    {% for withdrawal in withdrawals %}
    <tr>
      <td>{{ withdrawal.amount }}</td>
      <td>{{ withdrawal.requested_on|date:"Y-m-d H:i" }}</td>
      <td>{{ withdrawal.status }}</td>
    </tr>
    {% empty %}
    <tr>
      <td colspan="3">No withdrawal history available.</td>
    </tr>
    {% endfor %}
  </tbody>
</table>
{% endblock %}

<!-- prettier-ignore -->
{% extends "dashboard/staff/base_staff.html" %}
{% block title %}Failed Transactions{% endblock %}

{% block content %}
<h2>Failed Transactions</h2>

<table>
  <thead>
    <tr>
      <th>Transaction ID</th>
      <th>Merchant</th>
      <th>Amount</th>
      <th>Failure Reason</th>
      <th>Date</th>
    </tr>
  </thead>
  <tbody>
    {% for txn in failed_transactions %}
    <tr>
      <td>{{ txn.transaction_id }}</td>
      <td>{{ txn.merchant.name }}</td>
      <td>{{ txn.amount }}</td>
      <td>{{ txn.failure_reason }}</td>
      <td>{{ txn.date }}</td>
    </tr>
    {% endfor %}
  </tbody>
</table>
{% endblock %}

<!-- prettier-ignore -->
{% extends "dashboard/staff/base_staff.html" %} 
 {% block title %}Merchants{% endblock%} 
 {% block content %}
<h2>Merchant List</h2>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>KYC Progress</th>
      <th>Balance</th>
      <th>Status</th>
      <th>Notes</th>
    </tr>
  </thead>
  <tbody>
    {% for merchant in merchants %}
    <tr>
      <td>{{ merchant.name }}</td>
      <td>{{ merchant.kyc_status }}</td>
      <td>{{ merchant.balance }}</td>
      <td>{{ merchant.status }}</td>
      <td><a href="{% url 'merchant_detail' merchant.id %}">View</a></td>
    </tr>
    {% endfor %}
  </tbody>
</table>

{% endblock %}

<!-- prettier-ignore -->
{% extends "dashboard/staff/base_staff.html" %}
{% block title %}Payout Management{% endblock %}

{% block content %}
<h2>Payout Management</h2>

<table>
  <thead>
    <tr>
      <th>Merchant</th>
      <th>Amount</th>
      <th>Status</th>
      <th>Scheduled Date</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
    {% for payout in payouts %}
    <tr>
      <td>{{ payout.merchant.name }}</td>
      <td>{{ payout.amount }}</td>
      <td>{{ payout.status }}</td>
      <td>{{ payout.scheduled_date }}</td>
      <td>
        <button>Approve</button>
        <button>Trigger</button>
      </td>
    </tr>
    {% endfor %}
  </tbody>
</table>
{% endblock %}

{% extends "dashboard/staff/base_staff.html" %} {% block title %}Profile{%
endblock %} {% block content %}
<h2>User Profile</h2>
<div class="profile-info">
  <p><strong>Username:</strong> {{ user.username }}</p>
  <p><strong>Email:</strong> {{ user.email }}</p>
  <p><strong>First Name:</strong> {{ user.first_name }}</p>
  <p><strong>Last Name:</strong> {{ user.last_name }}</p>
  <p><strong>Is Staff User:</strong> {{ user.is_staff_user }}</p>
</div>
{% endblock %}

<!-- prettier-ignore -->
{% extends "dashboard/staff/base_staff.html" %} 
 {% block title %}Refund Processing{%endblock %} {% block content %}
<h2>Refund Processing</h2>

<table>
  <thead>
    <tr>
      <th>Transaction ID</th>
      <th>Merchant</th>
      <th>Amount</th>
      <th>Status</th>
      <th>Date</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
    {% for refund in refunds %}
    <tr>
      <td>{{ refund.transaction_id }}</td>
      <td>{{ refund.merchant.name }}</td>
      <td>{{ refund.amount }}</td>
      <td>{{ refund.status }}</td>
      <td>{{ refund.date }}</td>
      <td>
        <button>Process</button>
      </td>
    </tr>
    {% endfor %}
  </tbody>
</table>
{% endblock %}

<!-- prettier-ignore -->
{% extends "dashboard/staff/base_staff.html" %} 
{% block title %}
My Transactions
{% endblock %} 
{% block content %}

<h2>Staff dashboard/staff Overview</h2>

<div class="kpi-cards">
  <div class="kpi-card">
    <h3>Merchant Count</h3>
    <p>{{ merchant_count }}</p>
  </div>
  <div class="kpi-card">
    <h3>Pending Payouts</h3>
    <p>{{ pending_payouts }}</p>
  </div>
  <div class="kpi-card">
    <h3>Open Support Tickets</h3>
    <p>{{ open_tickets }}</p>
  </div>
  <div class="kpi-card">
    <h3>Suspicious Alerts</h3>
    <p>{{ suspicious_alerts }}</p>
  </div>
</div>

<!-- Date Filter Form -->
<form method="get" class="filter-form">
  <label>
    Start Date:
    <input type="date" name="start_date" value="{{ start_date }}" />
  </label>
  <label>
    End Date:
    <input type="date" name="end_date" value="{{ end_date }}" />
  </label>
  <button type="submit">Filter</button>
</form>

<table>
  <tr>
    <th>ID</th>
    <th>Base Amount</th>
    <th>Platform Fee %</th>
    <th>Platform Fee Amount</th>
    <th>Total</th>
    <th>Status</th>
    <th>Time</th>
  </tr>
  {% for txn in transactions %}
  <tr>
    <td>{{ txn.transaction_id }}</td>
    <td>{{ txn.base_amount }}</td>
    <td>{{ txn.platform_fee_percent }}</td>
    <td>{{ txn.platform_fee_amount }}</td>
    <td>{{ txn.total_amount }}</td>
    <td>{{ txn.status }}</td>
    <td>{{ txn.created_at }}</td>
  </tr>
  {% endfor %}
</table>

{% endblock %}

<!-- prettier-ignore -->
{% extends "dashboard/staff/base_staff.html" %}
{% block title %}Support Tickets{% endblock %}

{% block content %}
<h2>Open Support Tickets</h2>

<table>
  <thead>
    <tr>
      <th>Ticket ID</th>
      <th>Merchant</th>
      <th>Subject</th>
      <th>Status</th>
      <th>Last Updated</th>
    </tr>
  </thead>
  <tbody>
    {% for ticket in tickets %}
    <tr>
      <td>{{ ticket.id }}</td>
      <td>{{ ticket.merchant.name }}</td>
      <td>{{ ticket.subject }}</td>
      <td>{{ ticket.status }}</td>
      <td>{{ ticket.updated_at }}</td>
    </tr>
    {% endfor %}
  </tbody>
</table>
{% endblock %}

<!-- prettier-ignore -->
{% extends "dashboard/staff/base_staff.html" %}
{% block title %}Suspicious Activity Alerts{% endblock %}

{% block content %}
<h2>Suspicious Activity Alerts</h2>

<table>
  <thead>
    <tr>
      <th>Merchant</th>
      <th>Transaction ID</th>
      <th>Alert Type</th>
      <th>Date</th>
      <th>Details</th>
    </tr>
  </thead>
  <tbody>
    {% for alert in alerts %}
    <tr>
      <td>{{ alert.merchant.name }}</td>
      <td>{{ alert.transaction_id }}</td>
      <td>{{ alert.alert_type }}</td>
      <td>{{ alert.date }}</td>
      <td>{{ alert.details }}</td>
    </tr>
    {% endfor %}
  </tbody>
</table>
{% endblock %}
